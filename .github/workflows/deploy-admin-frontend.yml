name: Deploy Admin Portal to MonsterASP

on:
  push:
    branches:
      - main
      - feature/supabase-production-config
    paths:
      - 'frontend/admin-portal/**'

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: frontend/admin-portal/package-lock.json

      - name: Install dependencies
        working-directory: ./frontend/admin-portal
        run: npm ci

      - name: Build Angular app
        working-directory: ./frontend/admin-portal
        run: npm run build -- --configuration=production

      - name: Deploy to MonsterASP
        uses: SamKirkland/FTP-Deploy-Action@v4.3.5
        with:
          server: ${{ secrets.MONSTERASP_ADMIN_FRONTEND_FTP_SERVER }}
          username: ${{ secrets.MONSTERASP_ADMIN_FRONTEND_FTP_USERNAME }}
          password: ${{ secrets.MONSTERASP_ADMIN_FRONTEND_FTP_PASSWORD }}
          local-dir: ./frontend/admin-portal/dist/admin-portal/browser/
          server-dir: /site/wwwroot/
