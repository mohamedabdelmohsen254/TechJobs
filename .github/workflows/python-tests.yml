name: Python tests

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  unit-tests:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Compile helper package
        run: python -m compileall TechJobs-github/src

      - name: Run unit tests
        run: python -m unittest discover -s TechJobs-github/tests -v
